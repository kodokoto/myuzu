odd(x int) int
    if x%2==0
        return true
    else return false

res = odd(4)


f(x int, y int) int = x + y

f(x, y) = x + y

f(x int, y int) int
  return x + y

f(x, y)
  return x + y

if x or y
  # code


f(x int, y int) = x + y -> int 

f(x int, y int) -> int = x + y


f(x, y) = x + y

f(x int, y int) -> int
  return x + y

f(x, y)
  return x + y

if x or y
  # code