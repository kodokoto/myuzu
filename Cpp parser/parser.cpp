#include <iostream>
#include "parser.hpp"
#include <regex>

int main() {
    std::string expression;
    std::cin >> expression;
    std::cout << expression;
}

Expression parsePrimary(char input) {
    switch (input) {
        case
    }
}

int parseExpression(std::string input) {
    Expression lhs = parsePrimary(input.at(0));
}


