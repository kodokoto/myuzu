from lexer import Lexer

def check(token):
    if token.type =  
    return True

def p(condition):
    conditions = condition.split("|") # ["identifier"]
    
    return 

def def():
    match 
    if p("identifier lparen (parameter | some(parameter comma)) parameter rparen maybe(type decl)"):
        "signature (equals expression) | block"

        return {identifier: identifier.value}

def parameter():
    match
        case

def func_def(current_token):


    sig = FunctionSignature(name, parameters, span, return_type)

    return FunctionDecleration(sig, body, span)

def func_signature()

def span()

def identifier(token):

    return {identifier: _id, span: _span}

